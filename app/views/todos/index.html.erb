<%= link_to 'Add New Todo', new_todo_path %>

<ul class='todos'>
  <% @todos.each do |todo| %>
    <li class="<%= 'completed' if todo.completed? %>">
      <%= todo.title %>
      <%= button_to 'Mark completed', todo_completion_index_path(todo) %>
    </li>
  <% end %>
</ul>
